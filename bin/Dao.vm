#if (${PACKAGE_NAME} != "")package ${PACKAGE_NAME}.dao;#end

import com.test.swing.spring.domain.${DOMAIN_CLASS_NAME};

public interface ${DOMAIN_CLASS_NAME}Dao extends GenericDao<${DOMAIN_CLASS_NAME}, Integer>{
	${DOMAIN_CLASS_NAME} findBySsn(String ssn);
}
